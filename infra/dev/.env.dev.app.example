# ========================
# DEV - APP LAYER
# ========================

APP_ENV=development
NODE_ENV=development

# ---------- Database ----------
DB_HOST=postgres
DB_PORT=5432
DB_USER=dev_user
DB_PASSWORD=dev_password
DB_NAME=app_dev
DATABASE_URL="postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?schema=public"

# ---------- Redis ----------
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=1

# ---------- MinIO ----------
MINIO_ENDPOINT=http://minio:9000
MINIO_ACCESS_KEY=dev_minio
MINIO_SECRET_KEY=dev_minio_password
MINIO_BUCKET=dev-bucket

# ---------- Loki ----------
LOKI_URL=http://loki:3100

# ---------- External Integrations (DEV / SANDBOX) ----------
XENDIT_API_KEY=your_xendit_dev_key
WA_GATEWAY_URL=https://wa-gateway-dev.example.com
WA_GATEWAY_TOKEN=dev_wa_token
FCM_SERVER_KEY=your_fcm_dev_key

# ---------- App ----------
PORT=8080
LOG_LEVEL=debug

JWT_SECRET=dev_jwt_secret_change_me
APP_NAME=your-app-dev
